# P1 诊断输出与论文可视化说明

## 1. 当前诊断结果到底由谁输出

当前 P1 实验并未完整调用 Qwen2.5-7B 生成最终故障诊断文本。Qwen2.5-7B 目前只作为冻结文本编码器，离线把故障类别、故障机理和信号症状描述编码为文本语义向量；这些向量经投影后形成冻结故障原型。在线诊断时，轻量信号专家提取振动样本的故障嵌入，计算该嵌入与各故障文本原型之间的余弦相似度，再以最高相似度对应的类别作为分类结果。因此，现阶段的准确率、遗忘率和 BWT 均属于“轻量专家 + 冻结语义原型分类头”的结果，不是大语言模型生成式诊断结果。

这一阶段验证的是方法故事的前半段：小模型能否在工况连续到达时保持故障语义结构，并在不更新大模型的情况下完成跨工况分类。连续语义提示注入 Qwen、诊断解释生成、RAG 证据检索和自反思属于后续 P2 诊断头，不能用当前分类结果替代。

## 2. 参考论文的结果图如何构成证据

FD-LLM 使用原始数据的 t-SNE 分布说明类别混叠问题，再用模糊语义和问答示例展示大模型能够输出故障类别及维护解释，并通过骨干模型对比和模块消融量化各部分作用。它的优势是生成结果直观，但没有持续工况序列中的遗忘证据。

Risca 等（2025）围绕跨域持续学习绘制域级性能雷达图、训练阶段准确率轨迹、不同域顺序曲线、不同回放容量曲线和噪声鲁棒性曲线，并配合 ACC、LA 和 FM 表格。它说明持续学习论文不能只报告最终平均准确率，还要揭示每个域在学习新域后的变化，以及稳定性与可塑性之间的权衡。

CoLLM 将真实轨迹、大小模型预测轨迹、预测误差和不确定性放在同一图中，并进一步绘制置信度分箱内的样本占比与误差。该设计证明路由分数与实际可靠性相关，而不是只报告调用比例。对本项目而言，原型分类概率可先用于 specialist 置信度分析，但必须标明它尚未是 LLM 路由智能体的最终置信度。

DecAlign 使用混淆矩阵定位易混类别，并通过跨模态 t-SNE 及成对连线观察模态间距在消融前后的变化。对应到本项目，应展示信号故障嵌入与冻结文本原型在持续学习前后的相对位置，验证性能提升确实伴随语义空间稳定，而不是仅由分类边界偶然变化造成。

## 3. 已实现的可视化输出

运行 `scripts/visualize_p1_results.py` 后，现有结果可直接生成以下论文图：

- `fig01_strategy_summary`：比较最终平衡准确率、平均增量准确率、平均遗忘和 BWT。
- `fig02_stage_seen_accuracy`：显示随工况依次到达，已见工况平均性能如何变化。
- `fig03_final_domain_accuracy`：比较不同策略在每个工况上的最终保留与适应结果。
- `fig04_final_class_recall`：定位具体工况和故障类别的短板，避免平均准确率掩盖类别失衡。
- `fig05_accuracy_matrices`：以训练阶段 × 测试工况矩阵直接展示迁移与遗忘。
- `fig06_full_loss_curves`：检查语义对齐、跨工况约束和关系保持损失是否稳定收敛。

更新后的训练脚本还会保存每个阶段的样本级故障嵌入、预测概率、标签、样本编号、工况编号和文本原型。新实验完成后，可自动增加：

- `fig07_final_confusion_matrices`：各工况归一化混淆矩阵。
- `fig08_specialist_confidence`：置信度分箱可靠性与准确率-覆盖率曲线，为后续动态调用 LLM 提供阈值依据。
- `fig09_embedding_evolution`：联合 t-SNE 展示初始阶段与最终阶段的信号嵌入和冻结文本原型，观察类内聚合、类间分离及跨工况一致性。

## 4. 推荐的论文结果叙事

正文首先用阶段准确率曲线和准确率矩阵证明顺序训练出现遗忘，而完整方法能维持旧工况并吸收新工况；随后用最终域级结果、类别召回和混淆矩阵说明提升并非来自少数容易类别；再用嵌入演化图解释跨工况语义约束如何使同故障、不同工况的样本靠近统一文本原型；最后用置信度可靠性图说明何种样本可由小模型直接诊断，何种低置信样本应在 P2 阶段交给大模型进行提示增强诊断与解释。这样形成“性能提升 - 遗忘抑制 - 语义机制 - 协同接口”的完整证据链。
