# MultiDomainBearing 协议审计与修订方案

## 1. 审计结论

原18域协议由3种轴承、3组复合环境和2档转速构成。云端审计表明，域展开后共有861条记录，但仅对应573条独立原始记录；其中216条原始记录被分配到多个域，复用比例为37.70%，共有18组域对发生重叠。典型原因是环境组 A、B、C 共同包含 H，并分别共享 U1 或 M1。

因此，原18域并非18个统计独立的工况域。基于该协议得到的结果可以作为与既有实验设置一致的复现结果，但不能单独用于证明无数据重叠条件下的跨工况泛化能力。此前基于该协议获得的高准确率和较低遗忘率需要标记为重叠敏感结果。

## 2. 双协议设置

### 2.1 Overlap-18 复现协议

保留现有 `multidomain8` 数据集接口及18域定义，用于与 Risca 等的复合域实验设置保持可比。论文中应明确说明复合环境组存在原始记录共享，并同时报告重叠审计结果。

### 2.2 Atomic-48 主实验协议

新增 `multidomain8_atomic` 接口，将每一种实际环境状态作为独立工况。域由轴承类型、原子环境状态和转速组的笛卡尔积构成：

- 轴承类型：6204、N204/NJ204、30204；
- 环境状态：H、M1、M2、M3、U1、U2、U3、L；
- 转速组：slow（600、800、1000 rpm）和 fast（1200、1400、1600 rpm）。

共形成 3 x 8 x 2 = 48 个互斥域。域编号为 `d = 16b + 2e + s`，其中 `b` 为轴承索引，`e` 为环境索引，`s` 为转速组索引。每条原始记录只属于一个域，因而可用于独立跨工况持续学习结论。每个域和故障类别通常包含3个转速文件，以精确转速为分组单位分配到训练、验证和测试集，确保同一域内四个故障类别采用一致的训练、验证和测试转速，同时禁止同一原始文件的不同窗口跨集合出现。文件划分使用固定协议种子1729，不随模型随机种子改变；种子42、52和62仅用于估计模型训练与回放采样的随机波动。对于原始数据缺失导致仅有2个速度组的少数域—类别组合，采用1个训练组和1个测试组，验证集留空并在审计中显式记录。

## 3. 论文级实验顺序

1. 先运行 Atomic-48 源记录审计，要求 `reused_source_records=0`、`source_disjoint=true`。
2. 使用种子42运行 `se_gscl_full` 冒烟实验，确认48个阶段、4个类别和完整准确率矩阵均正常输出。
3. 在种子42、52、62上运行顺序微调、LwF、经验回放和 SE-GSCL 四种主方法。
4. 完成去跨工况约束、去历史关系保持和去故障—工况去相关三个消融。
5. 报告最终平均平衡准确率、平均增量准确率、平均遗忘、最大遗忘、后向迁移及逐域保留率。
6. 三随机种子只反映初始化和采样波动。主结果固定升序域流，随后增加反向域流或轴承交错域流，单独检验工况顺序敏感性。

## 4. 当前限制

MultiDomainBearing 包含三类结构不同的轴承。现阶段尚未逐一核实6204、N204/NJ204和30204的滚动体数量、节圆直径、滚动体直径及接触角，因此物理引导的P2局部症状模块仍应保持关闭。完成运动学参数登记和人工核验后，才能在该数据集上运行物理症状对齐与P3连续提示实验。
