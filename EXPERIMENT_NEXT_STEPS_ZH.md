# 下一轮实验：从探索结果到论文级证据

## 1. 已有结果的有效范围

### 1.1 P1 持续学习

此前在多工况顺序流上的三随机种子结果表明，SE-GSCL 相比顺序微调、LwF 和普通代表性回放具有更高的最终平均平衡准确率和平均增量准确率，并降低平均遗忘。以已汇总的一组多工况结果为例，顺序微调、LwF、普通回放和 SE-GSCL 的最终平均平衡准确率分别为 58.3%、57.1%、74.7% 和 78.2%，平均遗忘分别为 55.2、40.6、29.7 和 19.0 个百分点。

这支持的结论是：在当前数据协议、模型容量和记忆预算下，固定语义锚点、跨工况约束、关系保持和回放的组合比普通回放更能保留已学习工况。它尚不证明每个损失项各自有效，必须通过消融矩阵验证。

### 1.2 P2 局部症状

已有 CWRU 探索结果表明，物理软监督能使局部症状响应更接近预定义的故障—症状对应；全局身份、局部症状和融合后验的对比图可用于说明局部模块提供的是补充证据。现阶段不应把它表述为“物理可靠性已直接控制推理融合”：代码中物理量用于训练局部症状，推理期融合权重由源验证集选择的全局—局部后验规则决定。

### 1.3 P3 连续提示 LLM

在 CWRU 四工况的探索中，连续提示适配器在三个随机种子上得到 97.50% +/- 1.53% 的 LLM 分类准确率、97.92% +/- 1.28% 的平衡准确率，与上游融合诊断的一致率为 95.21% +/- 1.06%。诊断锁定解释的 160 样本运行中，控制后的 JSON、证据归属和维护策略一致性均达到 100%。

这些结果仅说明连续 token 接口与诊断锁定控制在当前较容易的 CWRU 协议中可以稳定运行。它不能单独证明 LLM 在更难工况上一定提高准确率，原因是 CWRU 类别较少、工况差异有限，且 P3 的输入已包含上游融合后验。论文必须报告“上游融合诊断”“结构化文本 LLM”“连续提示 LLM”三者的配对差异，并在第二数据集复验。

## 2. 先做的协议审计

MultiDomainBearing 的 18 个复合域由三种轴承、三组环境和高/低转速组成。原始环境记录在环境组之间存在重用，因此该 18 域构造可用于复现文献的复合域压力测试，却不能在未审计前直接等同于 18 个统计独立域。

运行 `scripts/audit_multidomain_overlap.py`，将报告每个原始记录落入的域数、任意两个域的共享记录数量和 Jaccard 重叠率。若存在重用，论文应明确称其为“overlap-sensitive compound-domain protocol”，并以 Paderborn 或 HUST 的源记录互斥协议作为独立泛化补充。

## 3. 推荐执行顺序

1. 对 MultiDomainBearing 完成重叠审计与 18 域 P1 四方法比较：顺序微调、LwF、普通回放、SE-GSCL。
2. 在相同 18 域流上完成三个 P1 消融：去跨工况对比、去历史关系保持、去故障—工况去相关。
3. 对 Paderborn 或 HUST 执行相同 P1 主比较和消融，作为源记录互斥的第二证据。HUST 主要验证跨转速流；Paderborn 主要验证载荷/速度/损伤条件的组合变化。
4. 仅在已核实轴承运动学参数的数据集上运行 P2：全局、弱局部、物理局部、语义保护局部和自适应融合。报告总体准确率、每类召回、冲突样本比例和局部覆盖率。
5. 在完成 P2 的同一测试协议上运行 P3：结构化文本、无辅助损失连续提示、完整连续提示、未锁定解释和锁定解释。分类结果、格式合规、证据一致性和维护策略一致性分开报告。

## 4. 当前不应做的事

- 不要将 18 域复合协议的高精度直接解释为独立跨设备泛化，除非重叠审计显示源记录互斥。
- 不要在 MultiDomainBearing 上调用物理引导 P2，除非已为 6204、N204/NJ204 和 30204 分别登记并核实运动学参数。
- 不要以“LLM 准确率高于小模型”作为唯一证据；P3 必须和上游融合诊断进行同样样本、同样测试域的配对比较，并报告 LLM 正确而上游错误、上游正确而 LLM 错误的样本数。
